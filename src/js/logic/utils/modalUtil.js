export function showAlertDialog(msg) {
  // TODO: Instead of javascript default modal dialog,
  //       use other modal module (FW, own codes, etc.)
  return alert(msg);
}

export function showConfirmDialog(msg) {
  // TODO: Instead of javascript default modal dialog,
  //       use other modal module (FW, own codes, etc.)
  return confirm(msg);
}
